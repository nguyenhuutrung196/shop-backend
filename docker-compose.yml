version: '3.8'
services:
    db:
        image: postgres:15
        container_name: shop_database
        restart: always
        environment:
            POSTGRES_USER: admin
            POSTGRES_PASSWORD: 123
            POSTGRES_DB: shop_db
        ports:
            - '5432:5432'
